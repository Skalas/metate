# Per-file hard line caps for skills/*/SKILL.md, enforced by `make budget` (wired into `verify`).
#
# Why: playbook prose has grown 98 -> ~1,400 lines, monotonically. `shrink-engine` removed 600
# lines of shell by harvesting the correctness INTO SKILL.md — complexity did not leave, it moved
# to the medium with no type system. TECH-DEBT's rule ("a change that adds prose to a
# skills/*/SKILL.md must remove prose") had nothing enforcing it.
#
# Raising a cap is allowed. Raising it must be an explicit line in the diff, argued for in the
# commit — that is the entire point. Lowering one after a real reduction is always welcome.
#
# Seeded 2026-09-01 from the field-audit pass (docs/FIELD-AUDIT-2026-08-31.md), at EXACTLY the
# post-audit line counts — zero slack, deliberately. That audit pass itself grew playbook prose
# 1,210 -> 1,403 lines (+16%) in a change whose own diagnosis was that this prose accretes. The
# caps start where that left off so the next change has to pay for its own additions.
# 2026-09-10, off-repo wiring (ADR-0003 phase 2): +1 on metate-start. The branch cut (step 6)
# gained two load-bearing lines — re-resolve $SPRINT after the cut, then claim the pending plan
# — and paid one back by shrinking the session reset, which the sprint-scoped dir now does by
# construction. Ordering, not prose: without them scope's plan lands in sprints/<base branch>.
skills/metate-start/SKILL.md 75
skills/metate-verify/SKILL.md 74
skills/metate-ship/SKILL.md 97
skills/metate/SKILL.md 201
skills/metate-build/SKILL.md 281
skills/metate-scope/SKILL.md 303
