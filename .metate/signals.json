{
  "signals": [
    {
      "id": "sig-validate-sh-silent-death",
      "capturedAt": "2026-08-25",
      "title": "tests/contracts/validate.sh dies silently under `set -euo pipefail` when a prose-drift grep finds nothing, so `make verify` fails with zero diagnostic output.",
      "repro": "Rename or renumber a step heading the validator greps for (e.g. change prep's `5. **Cut the branch**` to `6.`), then run `bash tests/contracts/validate.sh`. Exit code is 1 and nothing is printed — not even the script's own `die` message.",
      "evidence": "validate.sh:26-28 assigns via a pipeline: `prep_branch_line=\"$(grep -n '...' file | head -1 | cut -d: -f1)\"`. With `set -euo pipefail` (line 4) a no-match grep fails the pipeline, so the assignment itself aborts the script before control ever reaches the `|| die \"prep must cut the branch before seeding human gates\"` on line 31. `bash -x` trace ends at `+ prep_branch_line=` with no further output. The same construct is used for `prep_seed_line`. Fix: append `|| true` to the command substitution (or use a `grep ... || echo` fallback) so the intended die message actually prints. Found while diagnosing a red gate in sprint `discover-judgment`, where the missing message turned a one-line regex fix into a bash -x investigation.",
      "attribution": "exposed-latent",
      "severityGuess": "S2",
      "blocksDoD": false,
      "foundIn": "smoke:T9",
      "status": "promoted",
      "scope": "product",
      "tracker": "review-aperture sprint (validate.sh fix)",
      "disposition": "Promoted into the review-aperture sprint and fixed there; the `|| true` guard landed with that sprint."
    },
    {
      "id": "sig-bootstrap-untracks-deliberately-versioned-rule",
      "capturedAt": "2026-09-01",
      "title": "bootstrap.sh untracks .cursor/rules/codebase-memory.mdc even when the project deliberately committed it.",
      "repro": "In a repo that has `git add`ed .cursor/rules/codebase-memory.mdc, run `metate-init --update`. bootstrap's gi_ignore_untrack stages a deletion and adds a .gitignore rule, reverting the project's decision.",
      "evidence": "grouphead committed the file on purpose in PR #92 ('chore: versionar la config de metate y la regla de cursor'). Fleet propagation on 2026-09-01 staged `D .cursor/rules/codebase-memory.mdc` there and appended two ignore rules. Reverted by hand. gi_ignore_untrack assumes these paths are always installed tooling; a repo may legitimately vendor them.",
      "notes": "Fix direction: gi_ignore_untrack should no-op when the path is already tracked in HEAD — untrack only what bootstrap itself introduced. It must never stage a deletion of a file a human committed.",
      "attribution": "out-of-diff",
      "scope": "engine",
      "severityGuess": "S2",
      "blocksDoD": false,
      "foundIn": "live-test:fleet-propagation 2026-09-01",
      "status": "open"
    }
  ]
}
