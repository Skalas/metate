# Architecture Decision Records

The `decision` kind's completion artifact (see `metate-discover` → Candidate kinds). One file per
decision, numbered, never edited after acceptance — superseded instead.

| # | title | status |
|---|---|---|
| [0001](0001-slips.md) | Slips: typed, file-per-record state that stages block on | Proposed |
